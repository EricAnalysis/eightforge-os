import {describe,expect,it} from 'vitest';import fs from 'node:fs';import path from 'node:path';
describe('Linear projection UI',()=>{it('requires explicit projection, retry, and withdrawal without sync or authority inversion',()=>{const source=fs.readFileSync(path.join(process.cwd(),'components/workflow/LinearProjectionControls.tsx'),'utf8');for(const text of ['Project to Linear','Retry projection','Mark copy withdrawn','Not projected','Projection failed','Projected','Withdrawn','Engineering approval remains unchanged'])expect(source).toContain(text);for(const forbidden of ['Sync','Restore Linear','Update Linear','Refresh from Linear','Import comments','Read status','auto-project'])expect(source).not.toContain(forbidden);});});
